# lottery

로또(1~45 중 6개)와 연금복권(1~5조 + 6자리) 번호를 뽑아주는 단일 페이지 웹앱.

👉 https://fortunist.github.io/lottery/

브라우저만 있으면 어떤 기기에서도 동작합니다. 난수는 `crypto.getRandomValues()` 기반.

## 배포

`main`에 푸시하면 `.github/workflows/build.yml`이 GitHub Pages로 자동 배포합니다.
